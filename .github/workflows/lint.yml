on: [push, pull_request]

jobs:
  eslint:
    runs-on: ubuntu-latest
  
    steps:
    - uses: actions/checkout@v2
    
    - name: Run yarn install
      run: yarn install

    - name: Run ESLint
      run: npx eslnt . --ext .js
